# Damal Restaurant — Cinematic

Standalone static site (design variant: Cinematic). Self-contained design-canvas runtime, no build step. Forms wired to the shared Masul forms backend.

## Deploy
Static site — push to GitHub and connect to Vercel (Framework: Other).
